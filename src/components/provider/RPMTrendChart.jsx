// Folder: src/pages/provider/components/RPMTrendChart.js
import React from "react";
import { Card, CardContent, Typography } from "@mui/material";

const RPMTrendChart = () => (
  <Card>
    <CardContent>
      <Typography variant="h6">RPM Trend (Mock Chart)</Typography>
      <Typography variant="caption" color="textSecondary">
        [Graph showing BP, Glucose trends would go here.]
      </Typography>
    </CardContent>
  </Card>
);

export default RPMTrendChart;
